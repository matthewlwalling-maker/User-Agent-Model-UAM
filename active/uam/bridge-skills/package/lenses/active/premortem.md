# Premortem Lens

Before commitment, build the strongest case that the decision or artifact will fail and define an observable tripwire. Use for material `/design` and `/diagnose`, sparingly for high-stakes choices.

Output: decision, opposing case, likely failure causes, simpler version, kill condition.

