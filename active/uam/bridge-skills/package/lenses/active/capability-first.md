# Capability-First Lens

Derive required behavior from explicit and necessarily entailed goals before inspecting the current artifact as the solution. Use for material `/align`, `/design`, `/review`, `/compare`, and `/diagnose` work where anchoring could hide a missing capability.

Output: required capability model, coverage map, gaps, correct omissions.

