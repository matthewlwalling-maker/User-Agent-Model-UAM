# Generic Adapter

Status: placeholder.

Inject the active skill package anchors (`KERNEL.md`, `MANIFEST.yaml`, and `CHAIN_ROUTER.md`), exactly one selected command skill from `skills/active/uam-bridge-skills/commands/`, and only needed lenses. Do not inject the entire library by default.
